import { CorevaluefilterPipe } from './corevaluefilter.pipe';

describe('CorevaluefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CorevaluefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
